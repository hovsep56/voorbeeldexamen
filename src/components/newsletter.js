import React from "react"

const Newsletter = (props) => {
  return (
    <article className="newsletter">
      <h3>{/*title*/}</h3>
      <p>{/*information*/}</p>
      <a>{/*subscribe link title (voeg de url aan het href attribuut toe)*/}</a>
    </article>
  )
}

export default Newsletter
